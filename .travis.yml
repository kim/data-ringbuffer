language: haskell
install:  cabal install cabal-dev
script:   cabal-dev install --enable-benchmarks && cabal-dev build && cabal-dev bench --benchmark-options="+RTS -N"
notifications:
  email:
    - kim.altintop@gmail.com
